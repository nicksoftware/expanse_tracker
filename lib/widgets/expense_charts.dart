import 'package:flutter/material.dart';

class ExpensesCharts extends StatelessWidget {
  const ExpensesCharts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Charts go here');
  }
}
